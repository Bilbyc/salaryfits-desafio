model Conta {
  id Int @id @default(autoincrement())
  created_at DateTime @default(now())
  updated_at DateTime @updatedAt
  nome String
  email String
  senha String
  papel Role
}

enum Role {
  USUARIO
  ADMIN
}